// Define a function named make_shirt with parameters for size and message
function make_shirt(size, message) {
    // Print a message indicating the selected size and message for the shirt
    console.log("You selected \"".concat(size, "\" size shirt with the \"").concat(message, "\" prints on it."));
}
// Call the make_shirt function with specific arguments
make_shirt("Medium", "H A Y A");
